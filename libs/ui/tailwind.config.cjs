/** @type {import('tailwindcss').Config} */
const config = {
  presets: [
    //
    require('@ph/config/tailwind/tailwind.config.cjs'),
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
module.exports = config;
